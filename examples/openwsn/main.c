#include "openwsn.h"
#include "scheduler.h"

int main(void) {
//   board_init();
//   scheduler_init();
    openwsn_start_thread();
   //  puts("hello?!");
   // openwsn_init();
   // puts("and now?!");
   // scheduler_start();
   while (1); // this line should never be reached
}
